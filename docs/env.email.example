# Email Configuration for Email Verification
# Copy these to your .env file and update with your actual values

# Email SMTP Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here
EMAIL_FROM=your-email@gmail.com

# Frontend URL (used in email verification links)
FRONTEND_URL=http://localhost:3000

# For Gmail:
# 1. Enable 2-factor authentication
# 2. Generate an App Password: Account Settings → Security → App passwords
# 3. Use the app password as EMAIL_PASSWORD
# 4. Keep EMAIL_SECURE=false for Gmail SMTP

# For other providers, update HOST, PORT, and SECURE accordingly
